---
description: Build configuration and Xcode project settings - apply when building, running, or testing the iOS app
globs: ["DosMangos.xcodeproj/**", "*.xcscheme"]
---

# Build Configuration for DosMangos

Only run builds when the user explicitly asks; otherwise they will rely on SwiftUI previews and static checks.

## Xcode Project Details

Never use xcodebuild from the CLI, always try to use XcodeBuildMCP first.

- **Project Path**: `/Users/jorge/Developer/DosMangos/DosMangos.xcodeproj`
- **Scheme**: `DosMangos`
- **Platform**: iOS Simulator
- **Simulator**: iPhone 17
